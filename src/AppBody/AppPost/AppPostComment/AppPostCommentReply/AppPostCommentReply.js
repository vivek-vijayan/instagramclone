import React, { Component } from 'react'
import { useDoubleTap } from 'use-double-tap'

export default function AppPostCommentReply(params) {
    return (
        <div>
            reply
        </div>
    );
};